import './productForDash.css';

const ProductForDash = () => {

    return (
        <div className='productForDash'>
            ProductForDash
        </div>
    );
};

export default ProductForDash;

// Crear la tabla que obtiene los productos....
// Crear la tabla que obtiene los productos....
// Crear la tabla que obtiene los productos....
// Crear la tabla que obtiene los productos....